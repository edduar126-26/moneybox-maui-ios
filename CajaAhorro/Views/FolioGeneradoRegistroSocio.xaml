<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Money_Box.Views.FolioGeneradoRegistroSocio"
             BackgroundColor="#FAFAFA">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="btnDark" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#42424c"/>
                <Setter Property="TextColor" Value="#FFFFFF"/>
                <Setter Property="WidthRequest" Value="150"/>
                <Setter Property="HeightRequest" Value="60"/>
                <Setter Property="VerticalOptions" Value="Start"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
            <Style x:Key="btnCerrar" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#42424c"/>
                <Setter Property="TextColor" Value="#FFFFFF"/>
                <Setter Property="WidthRequest" Value="320"/>
                <Setter Property="HeightRequest" Value="60"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="Margin" Value="20"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal">
            <Image Margin="10" HeightRequest="30" Source="logo.png" HorizontalOptions="Start"/>
            <Label x:Name="NombreSociolbl" VerticalOptions="Center" Margin="10, 10" FontSize="16" FontAttributes="Bold" TextColor="Black"/>
        </StackLayout>
    </NavigationPage.TitleView>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Margin="20">
                <ContentView HeightRequest="55">
                    <Label  Text="Confirmación de Solicitud" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" HorizontalTextAlignment="Center" TextColor="Black"></Label>
                </ContentView>

                <Label FontSize="16" Text="Se ha generado su solicitud y enviado a la Administración de la Caja de Ahorro"></Label>
                <Label FontSize="16" Margin="0,20,0,1" HorizontalOptions="Center" TextColor="Black" Text="Número de Solicitud"></Label>
                <Entry FontSize="17" x:Name="FolioGeneradoMvtotxt" HorizontalTextAlignment="Center" IsEnabled="False" ></Entry>
                <Label FontSize="16" Margin="0,20,0,1" Text="Para cualquier aclaración favor de acudir con su Administrador de la Caja de Ahorro"></Label>
                <Label x:Name="lblAddComp" FontSize="13" FontAttributes="Bold"  TextColor="Black" HorizontalTextAlignment="Start" Text="¿ Desea agregar comprobante a solicitud ?"></Label>

                <Button x:Name="comprobante" Grid.Row="0" Grid.Column="0" Text="Comprobante" ImageSource="comprobante"
                            Clicked="CamaraBtn_Clicked" Style="{DynamicResource btnCerrar}" />

                <Button x:Name="AceptarBtn" Grid.Row="0" Grid.Column="1" Text="Aceptar" ImageSource="btnAceptar"
                            Clicked="AceptarBtn_Clicked" Style="{DynamicResource btnCerrar}" />

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>