<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Money_Box.Views.MovimientosPrestamos"
             BackgroundColor="#FAFAFA">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="btnCerrar" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#42424c"/>
                <Setter Property="TextColor" Value="#FFFFFF"/>
                <Setter Property="WidthRequest" Value="320"/>
                <Setter Property="HeightRequest" Value="50"/>
                <Setter Property="HorizontalOptions" Value="Fill"/>
                <Setter Property="Margin" Value="20"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <NavigationPage.TitleView>

        <ContentView  Padding="{OnPlatform Android='30,30,0,0', iOS='30,30,0,0'}">

            <Grid MinimumHeightRequest="44"
           ColumnDefinitions="Auto, *"
           ColumnSpacing="8"
           VerticalOptions="Center"
           HorizontalOptions="Start">


                <Image Grid.Column="0"
                HeightRequest="30"
                
                Aspect="AspectFit"
                Source="LogoMoneybox"
                Margin="0,0,4,0"
                VerticalOptions="Center" />


                <Label Grid.Column="1"
                x:Name="NombreSociolbl"
                VerticalOptions="Center"
                HorizontalOptions="End"
                Margin="0"
                FontSize="16"
                FontAttributes="Bold"
                LineBreakMode="TailTruncation"
                MaxLines="1"
                TextColor="White"/>
            </Grid>
        </ContentView>
    </NavigationPage.TitleView>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <ScrollView>
                    <StackLayout>
                        <ContentView HeightRequest="55">
                            <Label  Text="Tipos de Préstamo" FontSize="20" FontAttributes="Bold" HorizontalOptions="Fill" HorizontalTextAlignment="Center" TextColor="Black"></Label>
                        </ContentView>
                    </StackLayout>
                </ScrollView>
               
                <Picker Margin="5, 5" x:Name="PckrPrestamos" Title="Seleccione Tipo de Préstamos" ItemsSource="{Binding ListaPrestamos}" 
                        ItemDisplayBinding="{Binding Concepto}" HorizontalOptions="Center"></Picker>

                <Button x:Name="AceptarBtn" Text="Aceptar" 
                        ImageSource="btnAceptar" Clicked="BtnAceptarPrestamo" 
                        Style="{DynamicResource btnCerrar}" />
                
                <Button x:Name="RegresarBtn" VerticalOptions="Fill" Text="Regresar" ImageSource="btnRegresar"
                            Clicked="RegresarBtn_Clicked" Style="{DynamicResource btnCerrar}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>